# AI Workbench 项目交接文档

## 项目概览

- **项目名称**: AI Workbench (AI工作台)
- **定位**: AI 驱动的开发工具箱（Windows 桌面应用）
- **技术栈**: Tauri v2 + React + TypeScript + Vite + Rust + SQLite
- **工作目录**: 仓库根目录
- **应用路径**: `src-tauri\target\debug\ai_workbench.exe`（仓库内相对路径）

## 当前状态

- ✅ 主要功能可用；实体数据持久化到 SQLite（`%APPDATA%\com.ai-workbench.app\ai-workbench.db`）
- ✅ 前端 UI 状态（设置）使用 localStorage（Zustand persist）
- ✅ 侧边栏：AI 工作台 / 身份管理 / 开发环境 / 系统工具
- ✅ DeepSeek / Git 页侧栏保活（`.page-panel`，切走不卸载）
- ✅ Git：工作区分组、多仓变更列表、扫描子仓、批量身份、AI 提交说明
- ✅ Cloudflare Tunnel：多会话并行、Token / config.yml、双栏日志
- ✅ 阶段 8：系统托盘 + 全局快问（上下文芯片）+ 数据导出/导入 + 片段库
- ✅ Cursor 多账号：独立登录 + 工作区/Agent 数据共享；修复 BLOB 类型破坏导致的黑屏；启动更稳、聚焦更快
- ✅ 功能页完善（2026-09）：模型配置批量删除与视图持久化（分组改为纵向面板：组头健康计数失败 / 未测试、一键展开或折叠全部分组；扁平与分组共用 `auto-fill` 统一卡片网格，等宽等高，模型 ID / URL 在复制按钮内省略截断）；片段导入导出/克隆/实时预览；Hosts 立即备份与写入确认；内网穿透日志搜索/导出与浏览器打开；快问多轮追问/重新生成；DeepSeek 自定义端口；Git 仓库搜索与批量移除
- ✅ Git 账号域名绑定（v0.1.4）：按域名（origin）自动匹配账号，身份解析优先级 path > host > git config；全局 Git 身份查看/编辑；删除账号自动清理关联绑定
- ✅ 文档入口：根目录 `README.md`、本交接文档、`docs/cloudflared.md`、`docs/workbench-upgrade-plan.md`

## 已完成功能

### 1. AI 工作台

- **模型配置**: 多提供商配置、连接测试、同步到 DSH（`AIAssistant.tsx`）
- **DeepSeek 本地服务**: 安装/启动/停止/`@deepseek-ai/dsh` iframe（`DeepSeekHarness.tsx`，侧栏直挂）；空态内嵌安装按钮；可选自动启动（`workbench-dsh-autostart`，mount 后 500ms 且未运行才触发）；运行中每 8s 轮询 `list_dsh`，进程消失自动回落未启动态
  - 运行态顶栏与 iframe 合成单面板；检测中文案去重；侧栏切换保活不刷新 iframe
- **快问**: 托盘常驻 + `Ctrl+Alt+K` 迷你窗（`QuickAskApp.tsx`）；上下文芯片；多轮追问（最近 6 轮随请求携带）/ 重新生成 / 新对话；`Esc` / 关闭按钮走 `hide_quick_ask`；`tray.rs` / `tray/bubble.rs`
- **提示词优化**: `PromptOptimizer.tsx`；模型/模板下拉 Portal 定位，避免小窗裁切
- **片段库**: `SnippetsManager.tsx`；可插入快问

### 2. 身份管理

- **Cursor**（`CursorManager.tsx` + `src-tauri/src/cursor/mod.rs`）:
  - 多账号：每账号独立 `--user-data-dir`（`cursor-profiles/<accountId>`），登录态互不影响；一键切换 / 完成初始化 / 重新打开 Cursor
  - 工作区共享：`workspaceStorage` / `History` / `snippets` / `extensions` / `settings.json` / `keybindings.json` 链接到默认 Cursor；`globalStorage` 内机器级数据（`anysphere.*`、`conversation-search.db`、扩展存储）也已共享
  - 登录隔离：`state.vscdb`（含 `cursorAuth/*`）、`Network/Cookies`、`Local State`、`Local/Session Storage` 按账号独立
  - 其它：账号搜索/排序、打开配置目录、复制诊断信息、可联动 Git 身份、本机密码备忘
- **WorkBuddy**（`WorkBuddyManager.tsx` + `src-tauri/src/codebuddy/` + `wb_*.rs` + `wb_gateway/`）:
  - CodeBuddy 账号池控制台 + 进程内 OpenAI 兼容反代网关；分区：网关 / 账号池+签到 / 网关密钥 / IP 管控 / 日志与用量
  - **上游协议已全部真凭证实测**（端点清单见下文「WorkBuddy 网关要点」）：登录页、签到、签到状态、chat、成长端点全部读 `wb_settings.adapter_json`，抓包后填 JSON 即可，不需要重新构建；未配置时账号停在「未验证」、验证按钮禁用
  - 凭证本机明文入库（沿用 Cursor / 模型密钥约定），界面只回掩码预览
- **Git**（`GitManager` → 仓库 / 提交）:
  - **仓库**（`GitReposPage`）: `git_workspaces` 分组折叠；摘要/脏仓计数；扫描子仓；多选批量绑定；身份不一致警告
  - **提交**（`GitCommitPanel` + `CommitChangelist`）: 多仓勾选变更树、左右分栏 diff 高亮、暂存/丢弃/提交/推送/拉取、AI 生成说明、身份快速条
  - **报告**（`GitReportsPage` + `TestReportPanel`）: 左树仓库徽章与仓库级模式记忆；右侧概览 / 改动文件 / 提交 / API / AI 五视图；改动基准含未提交改动 / 相对基准 / 最近 N 个提交 / 指定日期区间（`resolve_date_window` 把日期窗口解析成 commit 范围）；文件搜索 + 层级 / 风险筛选；风险摘要可跳转定位，缺测数字与接口变更行均可下钻到文件列表；文件行路径拆成「目录 / 文件名」两段渲染，目录先让出宽度以保住文件名；AI 支持自定义 system 提示词与章节复制，并可粘贴需求 / 验收标准（`requirement` 参数，本机记忆、上限 8000 字符）以追加「需求覆盖对照」；「必测场景」按固定用例行格式渲染为 P0/P1/P2 卡片（后端 `output_rules()` 生成 ↔ 前端 `src/lib/reportAi.ts::parseAiLine()` 解析，**改格式必须同步**）；整份报告含需求原文可导出 Markdown；AI 结论自动落 `report_ai_history`（上限 50 条），AI 页可只读回看 / 复制 / 删除
  - 工作区筛选会限制可见 dirty；进入提交页时若当前 dirty 仓被滤掉会自动扩筛选；空态区分「筛选内无改动」vs「真正无改动」

### 3. 开发环境

- **运行时切换**: `VersionSwitcher.tsx`

### 4. 系统工具

- **小工具**（`DevTools.tsx` + `src/devtools.css`）：侧栏分组导航（转换/生成/解析/网络），10 个工具（JSON/编解码/时间戳/Cron/UUID/Hash/JWT/正则/端口进程/HTTP 客户端）；侧栏含搜索 + 最近使用；工具页头显示图标/名称/描述；键盘快捷键 1-9 快速切换；localStorage 持久化当前工具 + 最近列表；保活机制同 Git/DSH（`.page-panel`，切走不卸载）
- **Hosts**: 读写、管理员检测、备份/恢复
- **Cloudflare Tunnel**: 见下文「Cloudflare 要点」与 [docs/cloudflared.md](docs/cloudflared.md)
- **插件**: URL 书签 + WebviewWindow

### 5. UI/UX

- 自定义标题栏、可折叠侧边栏、亮/暗/玻璃主题、中英文 i18n
- Cloudflare 页双栏布局（左控右日志）
- 弹窗标题条统一（`ModalTitleRow`）

## 侧边栏导航结构

```
AI 工作台
├── DeepSeek
├── 模型配置
└── 提示词优化

身份管理
├── Cursor
├── WorkBuddy
└── Git 管理

开发环境
└── 运行时

系统工具
├── 小工具（JSON/编解码/时间戳/Cron/UUID/Hash/JWT/正则/端口/HTTP）
├── Hosts
├── Cloudflare
└── 插件

设置
```

## 技术架构

### 目录结构（摘要）

```
src/
├── components/
│   ├── DeepSeekHarness / AIAssistant / PromptOptimizer
│   ├── GitManager / GitReposPage / GitCommitPanel / CommitChangelist
│   ├── AccountManagerModal / RepoBindingModal / BatchIdentityModal / ScanReposModal
│   ├── CursorManager / VersionSwitcher / HostsManager / CloudflaredManager
│   ├── PluginBrowser / Settings / ConfirmModal / ModalTitleRow / DevTools
│   ├── devtools/ # JsonTool / EncoderTool / TimestampTool / CronTool / UuidTool / HashTool / JwtTool / RegexTool / PortProcessTool / HttpClientTool
├── core/          # store / types / storage / pathUtils
├── i18n/ + locales/
├── styles.css
└── App.tsx        # 侧栏路由；gitMounted / dshMounted / devtoolsMounted 保活（`.page-panel`）

src-tauri/src/
├── main.rs / lib.rs
├── db_commands.rs / git_commands.rs / ai_commands.rs / dsh_commands.rs
├── hosts_commands.rs / cursor_commands.rs / cloudflared_commands.rs
├── plugin_commands.rs / tool_commands.rs
```

完整树状说明见 [README.md](README.md)。

### 数据存储

- **实体数据**: SQLite（`rusqlite` bundled），`%APPDATA%\com.ai-workbench.app\ai-workbench.db`
- **前端 UI 状态**: localStorage（Zustand persist，键 `workbench-settings`）
- **主要表**: `git_accounts` / `git_repo_configs` / `git_host_configs` / `git_workspaces` / `host_profiles` / `web_plugins` / `plugin_states` / `recent_projects` / `cursor_accounts` / `ai_models` / `cloudflared_profiles`
- **Cloudflare 绑定字段**: `auth_mode`（`token` | `config`）、`token`、`config_path`、`hostname`、`local_url` 等

### IPC 约定

- 读写表：**仅** `db_load(table)` / `db_save(table, rows)`（Rust 侧表白名单）
- 例外：WorkBuddy 的 `codebuddy_accounts` / `wb_api_keys` / `wb_ip_rules` 与两张 FIFO 日志表**不进白名单**，全走 `wb_*` 专用命令——`db_save` 是整表覆盖写，对凭证表就是误删通道
- 勿再使用已废弃的 `sql_select` / `sql_execute` 描述

### Cloudflare 要点

- 运行时会话：`CloudflaredState.sessions: HashMap`（非单隧道）
- 命名隧道 id = profile id；临时隧道 id = `quick:<normalizedLocalUrl>`
- 事件：`cloudflared-log`（`{ id, tag, line }`）、`cloudflared-url`（`{ id, url }`）
- UI：`CloudflaredManager.tsx` 左栏控制 + 右栏日志过滤

### 页保活与 Git 扫描

- `App.tsx`：`gitMounted` / `dshMounted` + `.page-panel.is-active`，隐藏不卸载
- 仓库页摘要：并发池 + TTL；提交页对全部收藏仓算 summary，仅 dirty 仓拉 `git_status`
- 提交页 `filter` 保活会记住上次工作区；当前 dirty 仓被挡住时自动扩筛选

### Cursor 多账号要点

- **目录布局**
  - 账号 profile：`%APPDATA%\com.ai-workbench.app\cursor-profiles\<accountId>`（作为 `--user-data-dir`）
  - 轻量登录快照：`cursor-backups\<accountId>`（`auth.json` + `Local State` + `Network/Cookies` + `Local/Session Storage`）
  - 活跃账号指针：`cursor-active-account.txt`；共享目标：默认 Cursor 目录 `%APPDATA%\Cursor\...`
- **共享 / 隔离边界**
  - 共享（junction / 文件链接到默认 Cursor）：`User\workspaceStorage`、`User\History`、`User\snippets`、`extensions`、`User\settings.json`、`User\keybindings.json`、`User\globalStorage\storage.json`
  - 共享（`globalStorage` 内机器级数据）：`anysphere.cursor-agent-worker`、`anysphere.cursor-commits`、`anysphere.cursor-retrieval`、`conversation-search.db(+wal/shm)`、扩展存储目录
  - 不共享（按账号独立）：`state.vscdb*`、`globalStorage\backups`、`Network\Cookies`、`Local State`、`Local/Session Storage`
  - 入口：`link_profile_to_shared()` → `link_global_storage_to_shared()`；排除表 `GLOBAL_STORAGE_EXCLUDED`
- **黑屏根因（已修复）**
  - `sync_shared_state_to_profile` / `sync_profile_state_to_shared` 曾用 `sqlite_value_to_blob` 把 `ItemTable` 值统一转成 BLOB；VS Code 读回 `Uint8Array` 后 `JSON.parse` 抛 `Unexpected non-whitespace character after JSON at position 2`，`GlassWorkbench startup failed` → 黑屏
  - 修复：同步**保留原生 SQLite 类型**；新增 `repair_blob_typed_state()` 把「合法 UTF-8」的 BLOB 还原为 TEXT，在启动迁移与每次启动 Cursor 前兜底
- **数据安全**
  - `remove_tree_removing_links()` / `remove_path_for_link()`：删除 profile 或替换链接时**只摘链、不跟随**，绝不递归删除默认 Cursor 数据
- **性能**
  - `detect_running_cursor_data_dir()`（WMI）缓存 2.5s；`is_cursor_process_running()`（tasklist）缓存 500ms；`quit_cursor` / 启动后调用 `invalidate_cursor_probes()`
  - 退出后整定 `CURSOR_STOP_SETTLE_MS = 800ms`（原固定 2.5s）
  - 启动 Cursor 直接 `Command::spawn`（不再经 `cmd /c start`，避免空格路径与窗口站问题）
  - 前端窗口聚焦仅在登录标记实际变化时才写库
- **维护**
  - 启动迁移清理无主 profile（`cleanup_orphan_profiles`；账号表读取失败时不做任何删除，1 小时内改动过的目录跳过）
- **Composer 会话共享（2026-09 新增）**
  - 会话本体存于 `state.vscdb` 的 `composerHeaders` + `cursorDiskKV`（仅 `composerData:` / `composer.content.` 前缀），不走 ItemTable，需显式同步；`agentKv` 等派生缓存**不**同步
  - 链路（`prepare_profile_shared`，每次启动/切换执行）：默认目录→共享层 → profile→共享层（兜底回流）→ 共享层→profile（全部账号会话落地侧栏）；切换时 `sync_profile_state_to_shared` 额外回流本账号会话
  - 共享层 DB：`cursor-shared\User\globalStorage\state.vscdb`；登录态在 ItemTable，按 profile 隔离，不受共享影响
- **DB 瘦身（`slim_cursor_state_dbs` 命令，Cursor 页磁盘条入口）**
  - 流程：自动关闭 Cursor → 抢救式重建共享层 DB（先导出 composer 数据、删库、重 seed ItemTable、回灌会话，不丢数据）→ 默认库与各 profile 库 `wal_checkpoint(TRUNCATE)` + `VACUUM`
  - 背景：旧机制曾整库拷贝致共享层膨胀至 3.5GB（重建后 ~400MB）；实测 profile 库 85% 体积为 agentKv/bubbleId/checkpointId 活缓存，VACUUM 只能回收空闲页
- **同步性能（2026-09 优化）**
  - `export_cursor_disk_kv` 在 SQL 层用 `GLOB` 前缀过滤（读 ~48MB 会话数据而非 3.2GB 全表）
  - ItemTable / cursorDiskKV 导入用 `ON CONFLICT DO UPDATE ... WHERE value IS NOT excluded.value`：值未变不脏页、不动 mtime
  - 三段 composer 同步带指纹标记（`cursor-shared\.composer-sync\*.mark`，记录 mtime|大小|WAL 大小），源未变整段跳过——重复启动同步开销趋近于零
- **Cursor 页 UI（2026-09）**
  - 运行中徽标 + 10s 静默轮询 `is_cursor_running`；清理/瘦身期间禁用账号操作入口（防 DB 竞争）；状态标签胶囊化（`.tag-pill`）、账号行卡片化（scoped `.cursor-manager-page`）

### WorkBuddy 网关要点（2026-09 新增）

- **表不进 `db_load`/`db_save` 白名单**：`codebuddy_accounts`、`wb_api_keys`、`wb_ip_rules` 全走专用命令。白名单的 `db_save` 是「整表 DELETE + 逐行 INSERT」，让前端快照覆盖写这类表等于给凭证一个误删通道。
- **调用日志/签到历史是 FIFO 独立命令**（仿 `report_history.rs`）：请求日志上限 5000、签到 500，绝不能整表 load 进前端。
- **网关生命周期**：`std::net::TcpListener::bind` 成功即端口可用（失败直接报占用），再交给 `axum::serve`；停止用 `oneshot` + 3s 超时等待。LAN 开关改的是绑定地址，所以运行中会 stop→start；且**先校验有没有启用密钥再动运行实例**，避免「停下来了却启不动」。
- **SSE 只旁路不改写**：上游字节原样 `Body::from_stream` 透传，token 数由 `wb_gateway/sse.rs` 的扫描器从 `data:` 帧里挑最后一个 `usage`（分片跨块、非 UTF-8、超长帧都有界处理）。日志行在 `StreamPump::settle` 里落，`Drop` 兜底——客户端提前断开也要计一次调用。
- **stream:false 客户端适配（2026-09-26 补，唯一有意破例）**：上游 chat 只支持流式（stream:false 必回 400/11101），所以「SSE 只旁路不改写」在**客户端明确要非流式**这一种场景下主动破例：`wb_gateway/aggregate.rs` 的 `force_stream_body` 把请求体改写成 `stream:true`（并删 `stream_options`，防上游 400），响应帧经 `SseAggregator` 按 `choices[].index` 重组成完整 OpenAI `chat.completion` JSON 再返回（usage 取最后一帧、error 帧判失败、64MB 上限、读帧 120s 空闲超时）。`stream:true` 的透传路径一行未动；非 2xx（如 400）仍走 relay 原样透传。mock 上游的 e2e 测试在 `wb_gateway/tests.rs`（stream:false 得聚合 JSON、stream:true 逐帧原样）。**写 mock 上游的教训：响应写完必须 drain 到 EOF 再关——带着未读请求字节 drop socket 会触发 RST，全量并行跑测试时随机炸成「上游返回 502」**。
- **账号池路由**：粘性键是 (key, model)，保住上游 prompt 缓存；失败按 401/403=凭证失效(30min)、402/429=额度(10min)、5xx=瞬时(60s) 分级进惩罚箱，同一请求最多换 3 个账号；过期不只看 `status` 列，每次都用 JWT `exp` 重算。
- **扫码捕获**：登录窗 `build()` 必须在 **async 命令**里（同步命令里建窗在 Windows 会挂消息循环，同 `bubble.rs` 教训）；凭证回传走魔法域名 `aiwb-cb-capture.save` + `on_navigation`，因为运行时创建的窗口不保证有 IPC 桥。降级链：localStorage → fetch/XHR 请求头 hook → `document.cookie` → 手动粘贴（常驻兜底）；Cookie 库读取见下一条，已实现。
- **扫码抓到 ≠ 入库**：`ScanAddFlow` 抓到凭证后进 `confirming` 相位，把 名称/类型/凭证串/备注 预填进 `CredentialForm`，**用户点「确认入库」才调 `wb_add_account_manual`**（抓错头、抓到无关 token 时不至于污染账号池），入库后才 `wb_probe_account` 进 `verifying → saved`。表单本体在 `workbuddy/CredentialForm.tsx`，`ManualAddModal` 只是它的弹窗壳——改字段/校验只改一处。批量节奏：saved 相位「继续添加下一个」重开登录窗，名称自增（扫码账号 N）。
- **httpOnly 会话靠 Cookie 库兜底**（2026-09-26 补）：CodeBuddy/WorkBuddy 控制台登录后停在 `/profile/*`，鉴权是 httpOnly 会话 Cookie，页面脚本的 `document.cookie` 与请求头 hook **全都看不到**，于是 `wb_capture_cb_login_cookies` 直接读运行时 Cookie 库（含 httpOnly）。要点：① Windows 上 `WebviewWindow::cookies()` 是「往事件循环发消息 + 阻塞等回包」，**同步命令里调必死锁**，所以命令是 async 且把调用放进 `spawn_blocking`；② 按页面 host 做标准 Cookie 作用域过滤（`.codebuddy.cn` 命中 `www.codebuddy.cn`，`.tencent.com` 的 SSO Cookie 不会混进去，见 `domain_matches`）；③ URL 还含 `/login` 就判定未登录——那时只有 state/CSRF 噪声，绝不能当凭证递出去；④ **（v2 起改）**Cookie 不再无条件自动轮询入库——只有注入脚本的**页内会话校验 200**（脚本自己 fetch 一遍 credentialProbe，带 cookie + `X-Enterprise-Id`）通过才自动抓，否则废 Cookie 只会抢跑、把能抓到 API Key 的路径挤掉；失败相位仍有手动「抓取登录窗 Cookie」按钮。
- **上游真实入口（2026-09-26 实测；`loginUrl` 已证实，其余端点仍是猜测）**：登录表单（微信/手机号/邮箱/SSO）各平台**长得一模一样**，别拿页面文案判断走对了没有；真正区别在 `platform` 决定「登录完成后交给谁」：
  - `usercenter` → **个人版个人中心，这才是本功能该用的入口**。判据：未登录访问 `www.codebuddy.cn/profile/plan` 会自己跳到 `/login/?platform=usercenter&state=0&redirect_uri=https%3A%2F%2Fwww.codebuddy.cn%2Fprofile%2Fplan`——照抄这条 URL（模板已改成它），带 `redirect_uri` 才会停在窗口内、会话 cookie 留在我们的 webview 里。
  - `admin` → WorkBuddy **企业版管理后台**（页头 WORKBUDDY ENTERPRISE）。没有企业租户的账号扫完码会卡在「暂无企业账号 / 创建新企业以访问管理后台」。`copilot.tencent.com/login` 这个旧占位值就是 302 到这里，所以用户第一眼看到"企业账号"。
  - `workbuddy` → 桌面**客户端回传通道**（页面 ping `workbuddy-pc.woa.com`，二维码会话由客户端持有）。我们只是开了个网页、不是那个客户端，扫码必报「登录失败：请返回客户端点击重新发起登录」+ trace id。**这个坑踩过，别再往模板里写它。**
  - `open` / `open-platform` → 开放平台开发者主体（要选企业/个人开发者）。枚举里还有 personal/pro/ultimate/ide/cli/agents/genie/workbuddy-ai/… 表单相同、未验证。
  - 认证链：Keycloak `auth/realms/copilot/protocol/openid-connect/auth?client_id=console&scope=openid offline_access`（授权码，带 refresh token）→ `redirect_uri=…/console/accounts/.apisix/redirect`；登录后还有一步 `GET /console/accounts` 选「个人版/团队版」（`tagPersonal`/`tagTeam`）。⇒ 凭证大概率是 httpOnly 的 `.apisix` 会话 cookie，页面脚本抓不到——所以有了下一条的 Cookie 库兜底，**手动粘贴 Cookie 仍是最后兜底**。
  - 别和另一条路混：装好的 CodeBuddy IDE 里有 `https://api.lkeap.cloud.tencent.com/plan/v3/chat/completions`（另有 `/plan/anthropic/v1/messages`、`/coding/v3/chat/completions`）——那是 **Coding Plan 的 API Key 直连**端点，属于"有 key 就能用"，跟账号池登录态无关。
- **控制台 API 认 Bearer JWT，不认会话 Cookie（2026-09-26 用真凭证实测，别再走 Cookie 这条路）**：拿入库的整套 Cookie（含 `KEYCLOAK_SESSION`/`session`/`session_2`）重放 `GET /console/api/client/v1/api-keys`，返回与**完全不带凭证一字不差**——openresty/APISIX 的 `401 Authorization Required`；`/billing/*`、`/auth/realms/copilot/protocol/openid-connect/userinfo` 同样 authed≡anon。换成一个**JWT 形状**的 `Authorization: Bearer <alg:none>` 立刻从 401 变 **500**（网关真去验签了）⇒ 鉴权机制就是 Bearer JWT，Cookie 对这些路由毫无作用。所以 `credentialProbe` 模板已改成 `authorization: Bearer {token}` + `x-client-platform: web`（后者实测自 www.codebuddy.cn 自己的 axios 拦截器）。
- **令牌从哪来（同一次实测，从线上 JS 反出）**：`config-*.js` 里 `ee()` 读回跳 URL 的 **`?token=`** → `sessionStorage.setItem("growth-center-token", …)` → 立刻 `history.replaceState` 把参数抹掉；请求拦截器 `D()` 只在 `sessionStorage["growth-center-platform"]==="miniProgram"` 时才挂 `Authorization: Bearer`。⇒ 抓取必须覆盖 **URL 查询参数**与 **sessionStorage** 两处，只扫 localStorage 等于什么都没扫到。
- **「扫码永远抓到 Cookie」的另一半根因（2026-09-26 补，v2 捕获脚本）**：修完语法错误后仍然只出 Cookie——因为 ① **web 端根本不发 Bearer**（拦截器 `D()` 实锤只在 miniProgram 模式挂 `Authorization`，usercenter 网页全程 withCredentials + httpOnly Cookie，`X-Client-Platform: web` 恒发），头部钩子在 web 流程里注定落空；② 旧版 `ScanAddFlow` 的 `waiting` 相位**每 4s 自动读 Cookie 库**，登录一落地就把整串 Cookie 抢先塞进表单，把任何 token 捕获路径都挤掉。对策（`wb_capture.rs` 捕获脚本 v2）：登录窗脚本新增**响应体扫描**（fetch `clone().text()` + XHR `responseText`，匹配 `eyJ` 前缀 JWT 与 `access_token|api_key|apiKey|secret|token|key` 的 JSON 值——**API Key 只在创建响应里出现一次**，正好抓它）、**登录后自动跳 `/keys`**（「API 管理」页路由，实测自 usercenter 路由表 `ACCESS_KEYS:"/keys"`）+ 页内横幅提示创建 Key、**页内会话校验**（`VERIFY_URL` = adapter 的 credentialProbe URL，由 `capture_script_with()` 以 `serde_json` 转义注入，带 cookie 与 sessionStorage 里的 `profile-enterpriseId`），结果写 `window.__wbCaptureStats`（installed/ticks/calls/auth/bodyScans/cookieProbe/cookieOk…），主窗经 `wb_read_cb_login_probe` 轮询：`cookieOk` 才自动抓 Cookie（via=`verified-cookie`），`waiting` 相位显示统计行（脚本死活一眼可见）。**域名白名单（实测教训）**：登录页里嵌着 QQ 浏览器票据 iframe，`browsertdidticket.m.qq.com` 的响应体里就有 `"token"` 字段——无白名单时第一次扫码它第一个被 latch 入库（via=`body:browsertdidticket…`，状态"未验证"）。现在凭证捕获（头部 + 响应体）只认 `codebuddy.cn / copilot.tencent.com / lkeap.cloud.tencent.com`（`trustedUrl`，相对 URL 视为同源）；探针记录器刻意不设白名单——端点发现的候选可能在其它腾讯子域（如 admin 平台的 `api.account.tencent.com`）。改这段 JS 的验证顺序：`node --check` 提取脚本 → `node scripts/cap_sandbox_test.mjs`（vm 沙箱真跑：头部/响应体/白名单/校验/跳转/统计 15 例）→ `cargo test`。
- **注入脚本曾经整段是语法错误（这就是"扫码抓不到凭证"的真因）**：`CAPTURE_SCRIPT` 里 fetch/XHR hook 结束后多了一个提前的 `})();`，把后面的 localStorage/Cookie 抓取和轮询全甩到函数体外，末尾再补一个 `})();` ⇒ WebView2 静默吞掉语法错误，**Path 1/2/3 从来没跑过**，凭证只能靠 Rust 侧 Cookie 库兜底（于是永远抓到不可重放的东西）。已修，并用 `capture_script_is_one_iife_and_ends_with_it` 钉住结构（断言 `})();` 只出现一次且在最末）。以后改这段 JS，本地跑 `node --check` 提取出的脚本，别信"看着没问题"。
- **登录窗内端点自检 `wb_cb_login_selftest`**：把 `fetch(url,{credentials:'include'})` 丢回页面里跑（`eval` 注入 → 轮询 `window.__wbSelfTest` 取回，10s 超时），只回状态码 + 160 字节响应头几字节。它回答的是服务端探测永远答不了的问题：**这个端点在浏览器自己的会话下通不通**。浏览器 2xx 而我们重放 401 ⇒ 会话不可重放，换凭证类型；两边都 401 ⇒ 端点本身错了。URL 走 `serde_json` 转义 + 只允许 http(s) scheme。
- **接口探针（协议面板顶部，2026-09-26 补）**：别再猜端点。`CAPTURE_SCRIPT` 里除了凭证 hook 还挂了个调用记录器（`window.__wbProbe`）：fetch/XHR 的响应阶段把 `method / url / status / 认证头名 / 次数` 记进一个 40 条 FIFO 环形数组（只记**头名字**，值一个字节都不落盘；过滤掉 data:/blob:、静态资源、埋点域名）。前端 `wb_read_cb_login_probe` 用 `WebviewWindow::eval_with_callback` 把数组取回来——**不能用 `eval`**（它是 fire-and-forget，没有回包通道），且取回同样要 `spawn_blocking` + `recv_timeout(4s)`，登录窗跳走/已关时降级成空报告而不是挂住。UI 一行一条，点「探测/签到/对话」即把该条写进 `credentialProbe/checkin/chat`：观测到 `authorization` 之类就照抄成 `Bearer {token}`，什么都没带说明是浏览器自动送的会话 Cookie，生成 `["cookie","{cookie}"]`——**这正是判凭证类型的依据**。探测端点强制 GET，3xx 标灰（未登录的 302 到 Keycloak 不能当「有效」）。
- **另一条已知的机器对机器路径**：开放平台手册 `copilot.tencent.com/apiDocs/open-platform.html`（正文 `CodeBuddy-Open-Platform-Handbook.md`）走 OAuth2 `client_credentials` → `POST /oauth2/token` → `access_token`(JWT, `expires_in` 3600)，业务端点是 `copilot.tencent.com/v2/backgroundagent/agentmgmt/agents` 这类 **Agent API，手册里没有 OpenAI 兼容的 chat/completions**。装好的 CodeBuddy IDE 另有 `api.lkeap.cloud.tencent.com/plan/v3/chat/completions`（Coding Plan 的 API Key 直连）。网关最终转发哪个上游仍未定，但**必须是"凭证能搬出浏览器"的那条**。
  - **2026-09-26 直接打公网复核过状态码（结论已被当天晚些的 JWT 实测修正，见下一条）**：`/plan/v3/chat/completions` 无凭证与假 key 都回 `401 {"error":{"code":"not_authorized","type":"not_authorized_error"}}`（错误体就是 OpenAI 形状，路由活着）——但**它要的是腾讯云 API Key，Keycloak JWT 对它一律 401**，不能当扫码账号的探测端点。而 `copilot.tencent.com/api/sign-in`、`copilot.tencent.com/api/v1/chat/completions` 实测 **`404 {"error_msg":"404 Route Not Found"}`** —— 早期模板里那两个是我猜的，已证伪，别再用；`checkin` 没实测依据就留 `null`（配个打不通的 URL，一旦开自动签到，连败 5 次会把账号自动停用）。
  - **⇒ 凭证与端点的最终实测配对（2026-09-26 晚，全部真 JWT 打过）**：扫码捕获到的就是 **Keycloak JWT**（`body:www.codebuddy.cn/console/login/enterprise` 抓到，RS256，`azp=console`，约 55 天有效期）。配对结论：
    - `credentialProbe` = `GET https://www.codebuddy.cn/console/api/client/v1/api-keys?page=1&page_size=10&user_enterprise_id=personal`，头 `authorization: Bearer {token}` + `x-client-platform: web` —— 真 JWT **200**、无凭证 401、**缺 `user_enterprise_id=personal` 会 400**（空串也不行，值写死 `personal`）。GET 不烧 token，三态分明，是理想探针。
    - `chat` = `POST https://copilot.tencent.com/v2/chat/completions`，头 `authorization: Bearer {token}` + `content-type: application/json` —— 真 JWT 回 **400 {code:11101 "Non-stream chat request is currently not supported"}**（鉴权已过、只拒非流式），无/假凭证 401。**上游只支持流式，客户端必须 `stream:true`**；网关 400 原样透传、不会误标账号（`failure_status_code` 只认 401/403/402/429/5xx）。这组头来自 IDE genie 扩展 `buildAuthHeaders(!0,!1,!1)` 反编译：`Bearer accessToken` + `X-User-Id`/`X-Enterprise-Id`/`X-Domain`（实测 Bearer 一个头就够过鉴权）。
    - `tokenhub.tencentmaas.com/plan/v3/chat/completions`（IDE 里的 Coding Plan 代理）对 JWT **一律 401**，别配；`copilot.tencent.com/v2/models` 404，`models` 留 null 走 fallback。
  - **账号池的凭证不再要求「API 管理」页的 API Key**：那种 Key 走 lkeap/tokenhub 一族，与扫码 JWT 是两套体系。Cookie 仍不可入库（`wb_commands::missing_bearer_hint` 会在发请求前把 Cookie 错配直接报出来）；`UpstreamProfile` 四个端点字段都加了 `#[serde(default)]`，没把握的整段删掉即可。
- **签到防风控**：串行、账号间 2s、单轮内不重试；连败 5 次自动停用并在事件里带 `disabled` 让前端提示。**配置缺失不计入连续失败**（2026-09-26 补，`wb_checkin::apply_result` 加了 `counts` 参数）：没配 checkin 端点时点「全部签到」只记日志不累计——实测踩过没配端点点 3 次、离自动停用只差 2 次的坑；`checkin_fail_count` 被污染过要手动清零。
- **签到端点已实锤（2026-09-26 晚，真 JWT 打通）**：签到 = WorkBuddy 客户端（Electron，本机 `D:\w_app\WorkBuddy_Test\resources\app.asar`）「Buddy加油站」的每日领积分。asar 里 grep 到 `POST /billing/meter/daily-checkin`（领）与 `POST /billing/meter/checkin-status`（查状态，回 `today_checked_in/streak_days/week_progress`），httpService 全部走 `https://copilot.tencent.com`（与 chat 同源）。真 JWT 实测：无凭证 401；**daily-checkin 成功回 `{"code":0,"data":{"credit":100,"streak_days":10}}`**；**同一天重复签回 HTTP 400 `{"code":10001,"msg":"今天已签到，请明天再来"}`**——`wb_checkin` 对这种回包按「今日已签到」记成功（`is_already_checked_in`：400 + msg 含"已签到"），其余失败会把上游 `msg` 带进结果（`summarize_failure`），不再只显示"上游 400"。`checkin` 端点模板：`POST https://copilot.tencent.com/billing/meter/daily-checkin`，头 `authorization: Bearer {token}` + `content-type: application/json`，body `{}`。`summarize_success` 对成功回包显示「领取 100 积分，连登 10 天」。成长计划的 `/activity/growth/*`（streak/heatmap/tasks）也认 JWT，但没有主动签到动作——连登天数靠"登录且使用 WorkBuddy"被动累计，网关代签增不了活跃天数。**缺 checkin 端点时点签到只记日志、不累计连续失败**（`apply_result` 的 `counts` 参数）；CheckinPanel 在没配端点时禁用签到按钮与自动签到开关。
- **签到状态卡 + 成长面板（2026-09-26 补）**：`checkinStatus` = `POST /billing/meter/checkin-status`（body `{}`，回 `today_checked_in/streak_days/week_progress/total_credits`，真 JWT 实测）已写入协议模板；`wb_checkin_status` 命令复用签到 targets 串行 2s 逐账号查询，**只读——不写账号表、不计连续失败、不触发停用**；CheckinPanel 状态卡手动触发（单查/全查），**进页面不自动加载**（避免一进页就打上游）。成长家族 `GET /activity/growth/streak`（连登阶梯 tiers）与 `GET /v2/activity/growth/tasks` 走 `wb_growth.rs` / `GrowthPanel.tsx` 纯只读展示，**绝不自动 redeem**（规则禁止脚本化兑换）；任务返回形状未实测，解析宽松、失败降级为原因提示。adapter 新字段 `checkinStatus`/`growthStreak`/`growthTasks` 全 `#[serde(default)]`，旧 profile 不受影响（线上库 adapter_json 已补齐这三键，改前有 `.bak-adapter-*` 备份）。
- **小毛刺清理（2026-09-26 补）**：① IP 管控的防火墙命令端口跟随网关设置（不再硬编码 8787）；② 账号池名称列可编辑（ManualAddModal 复用为编辑模式，只改 label/notes、凭证留空即不动）；③ 网关日志面板加密钥 + 时间范围过滤（`wb_log_list` 后端本就支持，纯 UI 接线）；④ `wb_api_keys` 加 `rotated_at` 列（pragma_table_info 幂等 ALTER，reset 只换 key 不动 created_at，密钥表展示轮换时间）。
- **踩过的坑**：`useInvokeErrorTranslator()` 原先每次渲染都返回新函数，被放进 `useCallback` 依赖后导致 `useEffect` 反复重跑、刚 set 的错误提示被下一轮 `refresh()` 清空。现已在 hook 内用 `useCallback([t])` 记忆化——写自定义 hook 时注意返回函数一定要稳定。

### 快问窗要点（2026-09 修复，务必先读）

- **不能从命令线程建窗口**：`ensure_quick_ask_window` 里的 `WebviewWindowBuilder::build()` 在命令线程上**永不返回**（HWND 已创建、builder 挂住），`toggle_quick_ask` 因此永远走不到 `show()` —— 外部表现就是「点悬浮球 / 托盘「快问」/ `Ctrl+Alt+K` 全都没反应」。
- **对策**：启动后在**主线程预建**快问窗（`prebuild_quick_ask_window` → `run_on_main_thread`，延迟 1.2s 以免拖慢冷启动）；`toggle_quick_ask` / `show_quick_ask` 只负责显示/隐藏，窗口尚未就绪时**只调度预建、不阻塞**。
- **不要信任 tao 的可见性缓存**：tao 的 `apply_diff` 用内部缓存标志做差集，差集为空直接 return；本机 `ShowWindow` 会被系统忽略，缓存于是与真实窗口失配，之后所有 `show()/hide()` 都变成 no-op。统一走 `tray::bubble::vis::{really_visible, force_visible}`（`IsWindowVisible` 读真实状态 + `SetWindowPos` 兜底）。
- **前端隐藏也要走 Rust**：`getCurrentWindow().hide()` 同样会被上述缓存问题吞掉，`Esc` / 关闭按钮改为 `invoke("hide_quick_ask")`。
- **打包**：`src-tauri/src/bin/` 下的内部工具（`cursor_switch_test`）由 `dev-tools` 特性门控、默认不构建，因此不会进入安装包；需要时 `cargo build --features dev-tools`。
- **验证方式**（无需看终端）：启动后悬浮球应可见、快问窗隐藏；点悬浮球应显示、再点应隐藏；`Esc` 应隐藏；关闭主窗口应收托盘，双击快捷方式应唤回且始终只有 1 个实例。

### Rust 后端命令

```
数据库:     db_load / db_save
Git:        set_git_config / set_repo_git_config / get_git_config / get_repo_git_config
            pick_directory / git_is_repo / git_scan_repos
            git_repo_summary / git_status / git_stage / git_unstage
            git_commit / git_push / git_pull / git_diff / git_discard / git_commit_context
Hosts:      read_system_hosts / is_admin / backup_hosts_now（手动备份）/ write_system_hosts / list_host_backups / restore_host_backup
Cursor:     backup_cursor_data / restore_cursor_data / capture_cursor_account / inspect_cursor_backup
            get_cursor_login_status / is_cursor_running / quit_cursor / launch_cursor
            init_account_profile / finish_account_profile / get_cursor_profile_dir / migrate_account_to_profile
            switch_cursor_account / delete_cursor_backup / list_cursor_backups
            get_cursor_disk_usage / cleanup_cursor_full_backups / read_cursor_diagnostics
            slim_cursor_state_dbs
AI:         test_model_connection / generate_text / generate_text_stream（可选 history 多轮上下文）/ cancel_request
DeepSeek:   start_dsh / stop_dsh / list_dsh / check_dsh_port / check_nodejs_installed / get_dsh_version / get_dsh_latest_version / get_dsh_versions / install_dsh / update_dsh / sync_model_to_dsh
Cloudflare: cloudflared_status / install / open_download / pick_binary / set_binary_path / clear_binary_path / pick_config / start_quick_tunnel / start_named_tunnel / stop_tunnel / stop_all_tunnels / tunnel_status
插件:       download_web_plugin / read_plugin_file / delete_plugin_file
系统:       set_auto_start / get_auto_start / copy_to_clipboard / open_in_browser（仅 http(s)，通用浏览器打开）
托盘/快问:   open_quick_ask_with_text / tray_toggle_quick_ask / hide_quick_ask / open_main_deepseek
            set_quick_ask_bubble_visible / set_quick_ask_bubble_position / quick_ask_bubble_ready
```

## 构建与运行

### 开发模式

```powershell
cd <仓库根目录>
npm run dev
```

### 构建安装包（推荐）

```powershell
npm run tauri build
```

### 运行调试版本

```powershell
Start-Process ".\src-tauri\target\debug\ai_workbench.exe"
```

## 已知限制

1. **平台**: 目前仅支持 Windows
2. **插件**: 单页下载上限、不递归子资源；Webview 导航行为受 Tauri 限制
3. **AI 对话**: 无内置多模型流式对话，需通过 DeepSeek
4. **DeepSeek**: 依赖本机 Node.js + npm 全局包
5. **Cloudflare**: 依赖本机 `cloudflared`；域名路由在 Cloudflare 控制台配置
6. **敏感数据**: API Key / Tunnel Token 等明文存本机 DB
7. **Git 提交页**: 仅扫描收藏仓库；工作区筛选会隐藏范围外的 dirty（有空态提示）
8. **Cursor**: 同一时刻只运行一个实例（切换会短暂关闭）；AI 工作区数据中仍存放在 `state.vscdb` 的部分按账号独立（见「Cursor 多账号要点」的共享边界）
9. **WorkBuddy**: 六个端点 `loginUrl` / `credentialProbe` / `chat` / `checkin` / `checkinStatus` / `growthStreak`+`growthTasks` 已全部真凭证实测（见上面「凭证与端点的最终实测配对」「签到端点已实锤」「签到状态卡 + 成长面板」），协议模板（`ProtocolPanel.tsx` 的 `TEMPLATE`）已内置这套实测值。旧模板里的 lkeap `/plan/v3/*` 是**腾讯云 API Key 的路**，对扫码 JWT 一律 401，别再配回去。未配置协议时账号只能停在「未验证」，网关返回 503 而不是伪造成功。上游 chat 只支持流式——stream:false 客户端由网关聚合 SSE 后回完整 JSON（见「stream:false 客户端适配」）。凭证捕获链：localStorage / 请求头 hook → 响应体扫描（JWT）→ Cookie 库（仅页内校验 200 时自动）→ 手动粘贴兜底

## 待办与清理（历史项）

下列清理项在 ROADMAP 阶段 2 已处理；若本地仍见残留文件请对照仓库删除：

- `AIModelConfigModal.tsx` / `MarkdownContent.tsx` — 已移除
- 未使用翻译与依赖 — 已清理
- 旧 Git 三层导航（Workbench / Identity / QuickSwitch 等）— 已收敛为仓库 + 提交

## 下一步建议

- 维护 Cloudflare / 提示词 / Git 多仓流程的 i18n 与回归测试
- 按需补充 `docs/screenshot.png`
- 继续「文档与代码一致」：改功能时同步 README / 本文件 / ROADMAP

## 注意事项

- 构建产物 `dist/` 与 `src-tauri/target/` 已在 `.gitignore`
- 实体数据在 `%APPDATA%\com.ai-workbench.app\`
- 前端设置键：`workbench-settings`
- 更完整的产品说明见 [README.md](README.md)
